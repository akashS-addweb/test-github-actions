name: deploy-Dist-actions
on: [push,workflow_dispatch]
jobs:
    test:
       runs-on: ubuntu-latest
       steps:
           - name: Get code 
             run:  |
               echo "hello world!"
               echo "${{toJson(github)}}"
               git clone https://github.com/akashS-addweb/test-github-actions.git
